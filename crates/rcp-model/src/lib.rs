//! The RODECaster Pro II as this app understands it: the routing matrix, the
//! labels for things the console only numbers, and the commands that change
//! them. No I/O, so it builds and tests without a device attached.

pub mod command;
pub mod labels;
pub mod model;
