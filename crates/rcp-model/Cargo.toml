[package]
name = "rcp-model"
description = "Routing matrix, labels and commands for the RODECaster Pro II"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
rcp-proto = { workspace = true }
