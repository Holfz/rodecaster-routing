{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The app talks to the console over HID in Rust; the webview only needs to invoke commands, listen for the pushed state changes, drive the window buttons in its own title bar, and read or set whether the app starts with the desktop.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "autostart:default"
  ]
}
